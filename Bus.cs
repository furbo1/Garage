using System;
using System.Collections.Generic;
using System.Text;

namespace Garage
{
    class Bus:Vehicle
    {
        public Bus(string make, string type, string regNo, int numOfEngines, string fuelType, int numOfSeats, double length, bool hasEngine) :base()
        {
            
        }
    }
}
